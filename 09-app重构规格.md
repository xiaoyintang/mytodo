# App 主线功能规格（方案 A · 已定稿）

> 用法：把本文件放进项目根目录，对 Claude Code 说：
> **「读 09-app重构规格.md，先通读现有代码告诉我数据模型长什么样，再按阶段一实现。」**
>
> ⚠️ 本文件是**第二版**。上一版采用方案 B（底部导航重构成 任务/习惯/目标/记录），**已作废**。
> 现采用方案 A：**底部 tab 一个不动，目标做成 tab 栏上方的常驻作用域条。**

---

## 0. 决策记录：为什么选 A

### 上一版的诊断是错的

上一版认为"现有 tab 混了两套分类逻辑（日/周/记录是时间视角、习惯是对象类型）"，据此主张重构。**这个诊断不成立。**

现有 tab 的分类标准其实完全一致——**按「是否绑定日期」分**：

| tab | 本质 |
|---|---|
| 日 / 周 | 受日期约束的东西，两种粒度 |
| 记录 | 已发生的事实，同样按日期 |
| 习惯 | **不受日期约束的东西**（靠锚点触发） |

习惯单独成 tab，不是因为它是"对象类型"，而是因为它在时间视图里无处安放。**分类是齐的，没有需要根治的根因。**

### 由此推出目标该放哪

> **目标既不是"日期上的东西"，也不是"不受日期约束的东西"——它是这些东西的来源。**
> **它是一个正交维度。而正交维度的正确位置是「作用域」，不是「并列项」。**

这就是 Slack 的 workspace、Linear 的 team 切换器都在导航之上的原因：它们是**作用域**，不是并列的目的地。

### 补充理由

- **访问频率不对等**：稳定期，焦点地图梳理几个月一次，每周上限设完基本不动，目标增删几个月一次。给它 25% 的顶层导航过于奢侈。
  （建立期会高频，但**产品不该为建立期优化**。）
- **主线必须天天可见**：它是这次需求的起因。常驻条从任何页面都能看到"今天重点是什么"。

### 明确否决「方案 C：常驻条 + 目标 tab 并存」

**不要做。** 两个入口指向同一个东西 = 每次都要选一个 = 增加决策成本；而常驻条要永久占用手机上很贵的垂直空间，换来一个重复入口。

「其实不互斥、都要」是回避取舍的话术。**本 app 使用者的核心病症就是"什么都想要，结果什么都推不动"，不要在产品里重演它。**

---

## 1. 目标界面

```
┌────────────────────────────────────────┐
│ Todo                      2026年8月4日  │
│ 🎯 今天主线   求职面试 · 自媒体      ▸ │ ← 常驻一行
├────────────────────────────────────────┤
│                                        │
│            （当前 tab 内容）             │
│                                        │
├────────────────────────────────────────┤
│  [ 日 ]  [ 周 ]  [ 记录 ]  [ 习惯 ]      │ ← 一个不动
└────────────────────────────────────────┘
```

**常驻条的双重身份：**
1. 今日主线的日常露出（只读展示）
2. 目标管理的入口（点击 `▸` 进入）

**底部 tab 完全不动**：日 / 周 / 记录 / 习惯。

---

## 2. 三条不可违背的设计原则

### 原则一：常驻条必须是真作用域，不能只是个按钮

如果其他页面不真的被主线过滤，那一行就只是"带图标的入口"，作用域的正当性不成立。**所以第 5 节的四条过滤规则不能省。**

### 原则二：任务靠【日程】触发，习惯靠【锚点】触发

这是两套机制，**绝对不能混**。

直接后果：**习惯不受「今日主线」过滤，每天照常全部出现。**
一旦让日程去管习惯，习惯会在非主线日消失，整条链就废了。这一条是硬约束。

### 原则三：决策集中在周规划，日常执行零决策

使用者的核心瓶颈是**决策成本**（不是时间，不是体力）。他历史上所有失败的系统，都死在"某个环节要求他当场做判断"。

UI 上的落地：**常驻条上的今日主线是只读的，修改入口只有周视图。**
不要因为"用户可能想改"就在日视图加编辑入口——那正是要防的。

---

## 3. 通用约束

```
1. 零必填。任何新增字段都必须可跳过，不允许"必须选目标才能保存"。
2. 不做连续打卡天数(streak)、不做完成率、不做达标率、不做评分星级。
   习惯的统计只允许：累计次数、最近30天做了几天。
3. 不做打分输入。需要填 1–5 分的地方一律改成拖动、二选一或左右滑动。
4. 不加番茄钟、专注模式、游戏化、成就徽章。
5. 不引入"主线对象"这种新实体——"主线"只是"某天主推哪些目标"的说法，
   底层就是 Aspiration + 日期的关联，不要新建一张表叫 MainLine。
6. 移动优先（PWA，手机单手使用）。
7. 复用现有组件和数据层，不引入新的状态库或后端。
```

---

## 4. 数据层改动

```
Aspiration（目标）:
  + color:       string
  + weeklyLimit: number | null    // 每周投入天数上限 1–7，null = 不限

Task（一次性任务）:
  + aspirationId?: string         // 可选，不强制

TimeEntry（时间记录）:
  + aspirationId?: string         // 创建时从关联的 task / habit 【复制】过来（快照）
                                  // 没有关联对象的留空，不推导、不弹窗

Habit:
  已有目标关联，不变

DayPlan（新增，或复用现有日程结构）:
  date
  + primaryAspirationIds: string[]   // 今日主线，可多条
  + mustDoTaskId?: string            // 今日必做，每天最多 1 个
```

**新建任务时的目标下拉**：可以不选；默认沿用上次选择；不强制每次选。

---

## 5. 作用域过滤规则（原则一的落地，不能省）

| 页面 | 主线作用域怎么体现 |
|---|---|
| **日视图** | 非主线目标的任务**默认折叠**（可手动展开，不阻止） |
| **记录** | 本周投入图按目标聚合、按目标着色 |
| **周视图** | 每天一格排主线；底部本周额度条 |
| **习惯** | **不过滤**——刻意的，见原则二 |

做了这四条，常驻条才是真作用域。不做，它就是个装饰。

### 日视图折叠的判定规则（必须按这个来）

| 任务状态 | 处理 |
|---|---|
| `aspirationId` 在今日主线列表中 | 展开 |
| `aspirationId` 存在，但不在今日主线中 | **折叠** |
| **`aspirationId` 为空（未归属）** | **展开，不折叠** |

> **原则：隐藏必须有明确理由。**
> 折叠只用于"明确属于别的主线"的任务。未归属的一律可见——
> 否则那些事会因为没归类而永久消失，而它们往往正是临时冒出来的真事。

折叠区标题要显示数量，例：`其他目标的 6 项 ▸`

### 为什么 aspirationId 不设成必填

强制必填 = 每记一件事都要先做一个决定，那正好是使用者最贵的东西。
实际运转：

```
快速捕捉         → aspirationId = null（不问）
周规划那 40 分钟 → 批量归属（"清空收集口"那一步）
归属之后         → 才参与主线折叠
```

**归属这件事本身也被批量处理掉了。** 加上"新建任务时默认沿用上次选择"这条兜底，
实际为空的只有快速捕捉那一批，它们本来就该等到周规划再分。

---

## 6. 常驻条

- 位置：页面标题下方、tab 栏上方，**所有 tab 通用**
- 内容：`🎯 今天主线  求职面试 · 自媒体`
  - 用各目标的 color 做小圆点
  - 今天没排主线时显示：`🎯 今天还没排主线  去周视图排 ▸`
- **展示部分只读**，点右侧 `▸` 进入目标管理页
- 高度尽量压缩（手机上垂直空间很贵），一行搞定，不要做成卡片

---

## 7. 周视图（主线排期的主场）

- 每天一格，可排**多条**主线（不限一条）
- 底部**本周额度条**：
  ```
  求职 3/5    自媒体 5/7    小红书 2/1 ⚠
  ```
  （本周被排为主线的天数 / 该目标的 weeklyLimit）
- **超上限只标黄提示，不弹窗，不阻止保存**
- 未排期任务池，可拖进某一天

> 目的：避免一天什么都干一点，结果什么都干不好。
> 但**不做硬拦截**——硬拦截会让人绕过系统，提示才会让人自己调整。

---

## 8. 日视图

- 顶部不重复显示主线（常驻条已经有了，不要重复占空间）
- **「今天那 1 件必做」**：独立卡片，视觉上明显高一级（不是加个星标了事）
  - 全局每天只能有 1 个
  - 试图设第二个时提示"今天已经有必做事项了，要替换吗"，不允许并存
  - 完成后给一句确认文案，**不要显示完成率或百分比**
- **非主线目标的任务默认折叠**（可展开，不阻止）
- 任务卡片显示所属目标的 color 小圆点

---

## 9. 目标管理页（从常驻条点进去）

把现有「习惯 tab 下的『我的目标』子 tab」**整体搬到这里**，习惯 tab 只剩打卡，更简单。

**行为集群和焦点地图的现有逻辑原样保留，不要改动。**

### 目标列表 —— 「两条腿」

每一行长这样：

```
🔴 求职面试        任务 8 · 习惯 1        本周 4h20m
🟡 自媒体+AI       任务 0 · 习惯 2        本周 1h10m
🔵 小红书开店      任务 0 · 习惯 0        本周 0m
```

字段：名称、颜色、**未完成任务数 · 启用中的习惯数**、本周实际投入时长

**「两条腿」是什么：**

| 腿 | 是什么 | 作用 |
|---|---|---|
| **任务腿** | 一次性任务 | **推进**——把事情往前推 |
| **习惯腿** | 可重复行为 | **维持**——让它不掉回去 |

**缺任何一条都走不动：**
- 只有任务没有习惯 → 做完一波就停了，没有持续
- 只有习惯没有任务 → 天天在维持，但**从来没往前走过**

看上面那三行就能一眼诊断：
- 求职：任务腿满、习惯腿几乎空 → 能推进，但一停就散
- 自媒体：**任务腿是 0** → 有"每天试一个 AI 功能"这类习惯，但没有任何具体的事在推进，所以视频永远剪不出来
- 小红书：**两条腿都是 0** → 它其实没启动，只存在于脑子里

> **这两个数字并排放着，就是为了让空的那条腿无处遁形。**
> 不需要分析，扫一眼就知道这个目标为什么推不动。
>
> UI 上：某条腿为 0 时，把那个数字标成灰色或加一个淡淡的警示色，但**不要弹提示、不要打分**。

### 目标详情

- 愿望描述
- 行为集群 + 焦点地图（**现有功能，原样保留**）
- **「每周投入上限」设置**（1–7 天，或不限）← 新增
- 「它的任务」「它的习惯」**只显示数量和跳转入口，不放完整列表**
  （点击跳到对应 tab 的筛选视图，避免这个页面变成大杂烩）

---

## 10. 记录 tab

保留计时和补记，新增：

### 10.1 TimeEntry 的目标归属

```
创建 TimeEntry 时，从关联的 task / habit 把 aspirationId【复制】过来（快照）
没有关联对象的 → 留空
```

**就这一条，没有别的推导逻辑。**

- ❌ **不要**做"默认填今日主线"的兜底推导
- ❌ **不要**在开始计时前弹窗问"这属于哪个目标"
- ✅ 归属可以事后在记录列表里点一下改

> **为什么用复制而不是 join 查**：任务被删了，历史记录不会变成孤儿；
> 任务后来改了归属，**已发生的记录不该跟着变**——台账记的是当时的事实。
>
> **为什么绝对不能在计时前弹窗**：计时是使用者进入工作状态的扳机
> （"按下计时 = 已经开始了"）。任何在计时前的阻断，都会毁掉这个扳机。

### 10.2 本周投入图

按 `aspirationId` 聚合，横条图，**并排显示计划与实际**：

```
求职      排 5 天  ·  实际 4h20m  ████████
自媒体    排 3 天  ·  实际 1h10m  ██
小红书    排 1 天  ·  实际 0m
```

- 不做达标率、不做百分比、不加任何评价性文案
- **只把两个数字并排放着，让差值自己说话**

> 用途：检验周规划有没有落地。排了 5 天但实际投入 40 分钟——这个差值本身就是最有用的反馈。

---

## 11. 习惯 tab

- **「我的目标」子 tab 搬走**（去了目标管理页），习惯 tab 只剩打卡，反而更简单
- 每个习惯可关联一个目标（可选），显示目标色点
- **【硬约束】习惯的显示与触发不受「今日主线」影响，每天照常全部出现**
- 统计只显示：
  - ✅ 累计次数（"今年做了 143 次"）
  - ✅ 最近 30 天做了几天
  - ❌ 不做连续打卡天数
  - ❌ 不做完成率 / 达标率

> **为什么禁 streak**：streak 越长，断掉的代价越大；而断掉是概率事件，时间够长必然发生。
> 它的期望结局是"越成功崩得越惨"。而本 app 使用者的历史失败模式恰好就是全有全无崩溃
> （清单破一次就整份作废）。
> 用「累计次数」替代——只增不减，断了没有损失事件，成就感还在。

---

## 12. 明确不要做

```
✗ 不要动底部 tab（日/周/记录/习惯 一个不改，不增不减）
✗ 不要在常驻条之外再给目标一个 tab（否决方案 C）
✗ 不要在目标详情页放完整的任务列表
✗ 不要新建 MainLine 实体，主线就是 Aspiration + 日期的关联
✗ 不要在日视图或常驻条上允许修改今日主线（只能去周视图改）
✗ 不要对超周上限做硬拦截，只提示
✗ 不要让习惯受"今日主线"过滤
✗ 不要折叠"未归属目标"的任务（只折叠明确属于别的主线的）
✗ 不要给 TimeEntry 做"默认填今日主线"的推导，只从 task/habit 复制
✗ 不要在开始计时前弹窗询问目标归属（会毁掉计时这个启动扳机）
✗ 不要做连续打卡天数、完成率、达标率、评分
✗ 不要加番茄钟、专注模式、成就徽章
✗ 任何新增字段都不允许必填
```

---

## 13. 分阶段实施

**别一次全做。** 每阶段做完跑两天再进下一个。

### 阶段一 · 骨架
1. 数据层：Aspiration 加 color / weeklyLimit；Task 加 aspirationId；DayPlan 加 primaryAspirationIds
2. 常驻条（展示 + 入口），所有 tab 通用
3. 「我的目标」子 tab 从习惯 tab 搬到目标管理页

### 阶段二 · 调度
4. 周视图排主线 + 本周额度条 + 超限标黄
5. 日视图非主线任务默认折叠（**按第 5 节的三条判定规则，未归属的不折叠**）
6. 日视图「今天那 1 件必做」
7. 目标详情页的「每周投入上限」

### 阶段三 · 闭环
8. **TimeEntry 加 aspirationId，创建时从 task / habit 复制**（见 10.1；不做任何默认推导）
9. 记录 tab：按目标聚合的本周投入图，并排显示「排了几天 vs 实际时长」（见 10.2）
10. 目标列表的「两条腿」数字（见第 9 节，含 UI 示例）
11. 习惯统计改成累计次数 + 最近 30 天（如果现在有 streak，顺手拆掉）

---

## 14. 验收标准

**阶段一做完**，问：从任何一个 tab，是不是都能一眼看到今天主推什么？

**全部做完**，问这三个：

1. 早上打开 app，**不需要做任何决定**就知道今天干什么？
2. 目标列表上，能一眼看出哪个目标的**任务腿是空的**？
3. 一周结束时，能看出**排的主线和实际投入差在哪**？

三个都是"是" → 成了。
