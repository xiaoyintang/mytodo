# 我的行动力 App 是什么

> 当前版本：2026 年 8 月 11 日 · 已上线可用

我做的不是一个普通的 Todo List，而是一套给自己使用的“行为发生系统”。

它的核心目标只有一个：

> 不是让我更擅长列计划，而是让真正有价值的行为更容易发生。

我发现，“知道自己应该做什么”和“真的能做出来”之间隔着很多层问题：

- 目标太抽象，不知道从哪里推进
- 想出了一堆行为，却不知道哪个最值得做
- 一个行为本身有固定流程，却被误拆成许多互相竞争的行为
- 任务太大，执行时不知道第一步是什么
- 每次行动前都要重新决策，启动成本很高
- 计划被打乱后容易产生挫败感
- 做了很多事，却不知道时间花去了哪里
- 行动完成后缺少正反馈，很难形成重复

所以我参考了福格行为设计、GTD、OKR、柳比歇夫时间记录法、注意力残留和任务切换成本等理论，把它们组合成了一条完整动线。

## 整体结构

```text
愿望/目标
   ↓
关键结果（复杂目标可选）
   ↓
行为集群：有哪些行为可能推动它？
   ↓
行为包（可选）：这个行为是否有一套必须整体完成的固定流程？
   ↓
焦点地图：哪些行为影响力高，而且我做得到？
   ↓
分流
   ├─ 一次性行为 → 日程任务 → 继承固定流程 → 当前下一步
   └─ 可重复行为 → 习惯 → 锚点触发 → 记录与正反馈
                         └→ 必要时排入 Todo，并继承固定流程
   ↓
当天主线
   ↓
实际行动
   ↓
时间记录、行动反馈与复盘
```

## 1. 目标和关键结果

目标用于回答“我想让什么事情变得更好”。

但类似“找到好工作”“保持好心态”这样的目标太大，直接往下拆会出现二三十条行为，全部平铺在一起，用户很难理解这些行为分别在推进什么。

因此我增加了一个可选的“关键结果”层：

- 目标：我想去哪里
- 关键结果：发生什么变化，才说明目标真的被推进
- 行为：我具体做什么，才能促成这个变化

例如“找到好的工作”下面，可以有：

- 简历能获得更多目标岗位的面试机会
- 项目经历能够经得住面试官追问
- 开放性问题能够表达清楚
- 对行业和商业问题形成自己的判断

这些仍然不是行为，但它们能给下游行为建立归属和意义。

简单目标不强制增加关键结果，仍然可以直接拆行为，避免把个人工具做成填表系统。

关键结果现在也有 AI 辅助：

- 没有关键结果时，可以让 AI 一起发散
- 已有关键结果时，可以检查整组是否重复、缺失或误写成行为
- 新增单条时，可以检查它究竟是结果，还是任务、行为或愿望
- AI 只能提出“新增/替换”建议，不能未经确认直接修改
- 外部聊天 AI 也可以调整关键结果；导回时先预览原结果和新结果，再决定是否应用
- 关键结果与行为可以作为同一批变更原子应用，并且整批撤回

## 2. 行为集群和焦点地图

这一部分主要参考福格行为设计。

当用户面对一个目标时，先发散行为集群，而不是立刻决定“我要坚持什么”。

系统会先区分：

- 愿望：抽象方向，不能直接执行
- 成果：想得到的结果，仍然不能直接执行
- 一次性任务：完成一次即可
- 可重复行为：需要反复发生
- 要减少或停止的旧行为

只有真正可执行的行为才进入焦点地图。

焦点地图本质上是一个个人 ROI 筛选器，用户自己判断两个维度：

- 这个行为对目标的影响力有多大
- 以我当前的条件，我有多大可能做到

综合排序按照“影响力 60% + 能做到 40%”，但黄金行为不能只靠综合分，它必须同时满足：

- 影响力不低
- 我确实做得到

AI 不替用户判断影响力和可行性，因为这些高度依赖个人处境。AI 主要负责帮助识别：

- 这是不是一个行为
- 表达是否足够具体
- 是否缺少完成边界
- 是否需要继续拆小

### 行为备选与固定流程不是一回事

焦点地图比较的是不同的“行为方案”，不是一个方法内部的操作步骤。

例如：

> 难受时做一遍 15 分钟的第三人称书写

这是一条需要整体判断 ROI 的父行为。如果这套书写法有 7 个固定步骤，7 个步骤缺一不可，它们就不是 7 个候选行为，也不应该分别拖 7 次影响力和可行性滑块。

因此行为卡现在可以保存一份“固定流程”：

- 父行为整体参与焦点地图评分
- 流程步骤只说明怎样完整执行，不单独评分
- 支持手动添加、一次粘贴多行、AI 拆解、编辑、删除和拖动排序
- 固定流程是可复用模板，不带完成状态
- 行为真正排进 Todo 时，模板才复制成可以逐项勾选的任务步骤

如果内置 AI 的上下文不够，App 还可以把目标、关键结果、已有行为和固定流程完整导出到 Claude、ChatGPT 等聊天模型中讨论。

讨论结束后，可以把结构化变更带回来：

- 新增或替换关键结果，并附上可验收的达成证据
- 新增行为，或逐字匹配原行为后进行替换
- 重新分配行为所属的关键结果
- 新增、替换、重排或清空行为的固定流程
- 同一个父行为可以保持不变，只更新它的流程

导回前会显示例如“原 7 步 → 新 3 步”以及完整的新顺序。省略流程表示保留旧流程，只有 AI 明确写出“固定流程：清空”才会删除。整批变更确认后一次应用，并且可以撤回。

为了避免 AI 再次生成几十条内容，“带去聊”的提示词还会要求：

- 上下文充分时允许跳过重复提问
- 两个导入区块合计不超过 12 条，宁可少而准
- 行为顺序只是尝试建议，不能代替焦点地图评分
- `[停止]` 只能用于当前真实发生、具体且可观察的行为，不能写成“停止焦虑”

也就是说，我没有试图把整个 App 变成聊天界面，而是让：

- LUI 负责探索、讨论和发散
- GUI 负责保存结构、比较、排序、执行和记录

## 3. 一次性任务、任务拆解和下一步行动

一次性行为可以直接加入日视图。

如果它在焦点地图里已经保存了固定流程，排期时会自动把流程复制成任务步骤，不需要再拆一次。

任务的标题允许写成一个成果，例如“发布一期视频”。因为现实中人们经常以成果组织工作，不必强迫所有任务标题都写成原子动作。

但是到了执行现场，系统会要求找到“当前下一步”。

因此任务内部可以：

- 手动添加行动步骤
- AI 拆成步骤
- 拖动调整步骤顺序
- 在当前下一步前临时插入更早的一步
- 检查下一步是否足够清晰
- 逐步完成，而不要求一开始就把整个任务拆完

这里吸收了 GTD 的核心思想：

> 下一步行动不是另一种任务，而是当前任务中，下一件真正可以开始做的事。

任务拆解产生一组行动步骤；排在最前面的未完成步骤，就是当前下一步行动。

这里区分“模板”和“执行实例”：

- 焦点地图里的固定流程是以后反复使用的做法模板
- Todo 里的行动步骤是这一次执行的副本，可以独立打勾、编辑和重排
- 后来修改模板，不会追溯篡改已经排出的任务
- 习惯以后再次排入 Todo 时，会读取最新版流程

这样既能看到成果，又不会在真正行动时继续思考“我现在该干什么”。

## 4. 日视图和主线

日视图不是要求所有事情都规定具体时间。

任务分为：

- 不限时段：当天完成即可，优先展示
- 有明确时间：按照时间顺序展示
- 从习惯手动排到今天的任务
- 属于当天主线目标的任务

没有具体时间的任务会进入“不限时段”，而不是被强行塞进晚间；主线相关且不限时的任务会优先展示，让当天最重要的方向更容易被看到。

“主线”来自注意力残留和任务切换成本的考虑。

用户可以提前决定今天主要推进哪些目标。执行时优先看到与主线相关的任务，减少在不同方向之间来回切换。

主线入口固定在“今天、本周、记录、习惯”四个 Tab 的相同位置；它既用于提醒当前方向，也可以进入目标和焦点地图，不需要在不同页面重新寻找入口。

我的原则是：

> 决策尽量批量提前做，真正行动时尽量不再动脑选择。

同时我不想使用僵硬的日程安排。任务时间发生变化，不应该让整份计划看起来“失败了”。

## 5. 习惯系统

可重复行为可以从焦点地图进入习惯页。

习惯会：

- 按所属目标分组
- 显示它服务的关键结果
- 按焦点地图综合评分排序
- 支持设置锚点，即“在什么事情之后做”
- 区分计次型和时长型
- 必要时一键排入当天 Todo

有些行为虽然需要重复，但每一次又像一个任务，例如投简历、改简历。它们可以保留在习惯系统里，同时手动加入当天任务。

习惯页本身只负责记录行为是否发生，不展开固定流程；但习惯保留对原行为模板的引用，因此从习惯排到今天时，Todo 仍然会获得那份流程。

完成习惯后，不只是按钮变色，而会明确告诉用户：

- 这次行动已经真实发生
- 它正在推动哪个目标
- 当前累计发生了多少次
- 可以顺手记录它带来的正向影响，例如更轻松、更清醒、更踏实

我刻意没有把“连续打卡天数”作为核心指标，因为断签会把原本的进步变成一次损失事件。

这里更强调：

- 累计发生次数
- 最近 30 天实际发生过多少天
- 行动带来了什么正向影响
- 中断不会把此前积累清零

## 6. 时间记录与复盘

这一部分参考柳比歇夫时间记录法。

App 不强迫用户预先规定每一分钟该做什么，而是记录实际时间去了哪里。

用户可以：

- 开始正事、娱乐或休息计时
- 用自然语言补记时间
- 使用 AI 解析复杂表达
- 把时间关联到任务和目标
- 查看当天与本周汇总

今日和本周使用一致的三个视角：

- 目标投入：时间分别花在了哪些目标上
- 时间构成：正事、娱乐、休息
- 活动明细：具体做了什么

这样用户不仅知道“完成了几项 Todo”，还知道自己真实投入了什么。

## 7. AI 在这里扮演什么角色

我不希望 AI 代替用户做所有决定。

AI 更适合：

- 帮助发散关键结果和候选行为
- 判断一句话是愿望、成果还是行为
- 把模糊表达改得更清楚
- 拆解任务
- 检查当前下一步行动
- 解析时间记录
- 辅助分类
- 在完整对话中检查关键结果结构，并把变更带回 App
- 为一个父行为整理完整、有序的固定流程

但以下判断仍然交给用户：

- 哪件事对自己真正有影响
- 自己当前能不能做到
- 哪个目标值得成为主线
- 是否采用 AI 的新增或替换建议

所有重要 AI 修改都必须先展示建议，用户确认后才生效。

外部 AI 导回遵循几个安全原则：

- 只能新增或替换，不能直接删除现有内容
- 替换必须引用原文并匹配到真实条目
- 关键结果、行为、归属和固定流程都要先预览
- 只修改固定流程时，保留父行为评分、习惯和关联
- 已排出的历史任务不被追溯覆盖
- 一批修改只生成一个撤回快照

## 8. 当前产品形态

目前 App 已经不是只有日视图和周视图的原型，而是包含四个主要工作区：

- 今天：执行当天任务、查看当前下一步、处理不限时段与有时间任务
- 本周：安排主线和观察一周任务
- 记录：计时、补记与查看日/周时间汇总
- 习惯：按目标和关键结果组织可重复行为，记录发生与正向影响

目标中心负责目标、关键结果、行为集群、焦点地图和固定流程。任务详情负责行动步骤和当前下一步。

界面同时适配手机与桌面：手机保持单列和紧凑操作，桌面会使用更宽的 Web 布局；长标题使用快速自定义悬浮提示，不依赖浏览器原始的延迟 tooltip。App 还支持 PWA、localStorage 持久化以及可选的多设备同步。

线上地址：<https://mytodo-brown.vercel.app>

## 我真正想解决的问题

这个 App 不是为了让我管理更多事情，而是试图依次消除行为发生之前的阻力：

- 不知道为什么做 → 目标和关键结果
- 不知道可以做什么 → 行为集群
- 不知道哪个值得做 → 焦点地图
- 一个方法被拆得支离破碎 → 父行为和固定流程
- 事情太大 → 任务拆解
- 不知道现在做哪一步 → 下一步行动
- 每次都要重新决定 → 主线和提前排期
- 忘记行动 → 锚点
- 做完没有感觉 → 庆祝与正向影响
- 不知道自己是否前进 → 行动记录和时间复盘

所以它更接近一个“个人行为操作系统”，而不仅是待办事项 App。

它仍然处于持续迭代阶段，很多功能并不是一开始规划出来的，而是我自己真实使用时，发现“这里仍然不能帮助行为发生”，再继续补上的。

如果你要评价它，请不要只从普通 Todo App 的功能完整度出发，也请从下面几个角度判断：

1. 这条动线是否真的降低了从目标到行动的认知成本？
2. 哪些环节确实促进了行动，哪些只是增加了填写负担？
3. 目标、关键结果、行为、任务、下一步和习惯的边界是否清楚？
4. 是否存在功能重复、概念过多或层级过深的问题？
5. 怎样在不牺牲这套行为逻辑的前提下，让产品更简洁、更自然、更好用？
6. “候选行为”和“行为内部固定流程”的边界是否足够直观？
7. AI 的职责是否被限制在真正擅长的探索、表达和结构整理，而没有越权替用户做价值判断？
