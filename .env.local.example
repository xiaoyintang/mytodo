# AI 自然语言解析配置（OpenAI 兼容接口）
# 复制为 .env.local 并填入你的 key；不配置时自动降级为内置规则解析
# Vercel 部署时在 Project Settings → Environment Variables 里配置同名变量

# DeepSeek（https://platform.deepseek.com）
LLM_API_KEY=sk-xxxxxxxx
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-v4-flash

# 硅基流动（https://cloud.siliconflow.cn）改用下面两行
# LLM_BASE_URL=https://api.siliconflow.cn/v1
# LLM_MODEL=deepseek-ai/DeepSeek-V3

# ---- 多设备同步（同步码）----
# 需要一个键值库（Upstash Redis / Vercel KV，免费）。
# 推荐：Vercel 项目 → Storage → 新建 Upstash（KV/Redis）→ 连接到项目，
#       环境变量会自动注入（KV_REST_API_URL / KV_REST_API_TOKEN），无需手填。
# 若直接用 Upstash 官网，把下面两行填上（本地开发想测同步也填这里）：
# KV_REST_API_URL=https://xxxx.upstash.io
# KV_REST_API_TOKEN=xxxx
# 不配置时同步功能自动关闭，App 仍可正常本地使用。
